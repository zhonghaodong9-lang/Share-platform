services:
  - type: web
    name: global-news-platform
    env: node
    plan: free
    buildCommand: ""
    startCommand: node server.js
    envVars:
      - key: PORT
        value: "10000"
      - key: DATA_DIR
        value: /var/data
    healthCheckPath: /
